/**
 * Didban — Entry-Point des Workers (CLI).
 */

function main(): void {
    console.log('Didban worker starting...')
    console.log('Phase 1: Grundgerüst steht — Crawl-/Diff-Logik folgt.')
}

main()
